@model List<Services>

<div class="container">
    <div class="headings">
        <h2 class="heading">Service List</h2>
    </div>
    <div class="row">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="ServicesAdmin" asp-action="Create" class="btn btn-primary admin-create-button">
                    Create New Service
                </a>
            </div>
        </div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Service Name</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model.OrderBy(u => u.Id))
                {
                    <tr>
                        <td scope="row"> @obj.Id</td>
                        <td>@obj.ServiceTitle</td>
                        <td>
                            <a asp-controller="ServicesAdmin" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2 admin-button">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>
                            <a asp-controller="ServicesAdmin" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2 admin-button">
                                <i class="bi bi-trash-fill"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>